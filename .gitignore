# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Backend config (real DB credentials - copy api/config.sample.php instead)
api/config.php

# Backend PHP dependencies (run `composer install` inside api/, or see
# README - cPanel deployment needs this uploaded, not installed there)
api/vendor

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
