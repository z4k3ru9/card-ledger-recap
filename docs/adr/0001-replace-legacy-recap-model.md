---
status: accepted
---

# Replace the legacy recap model

PackTally will replace the single shared-login monthly JSON-blob model with normalized Accounts, Trips, Trip Members, Expenses, Allocations, Rate Snapshots, and Audit Events. Existing recaps are preserved exactly as read-only Legacy Recaps rather than guessed into participant-aware data, because that mapping cannot be made truthfully.
